[build]
  publish = "."
  # Skip rebuild kalau commit cuma ubah data.json (GitHub Action commit tiap minit).
  # Netlify cuma rebuild bila index.html / fail lain berubah -> jimat build-minutes.
  ignore = "git diff --quiet HEAD^ HEAD -- ':(exclude)data.json'"
